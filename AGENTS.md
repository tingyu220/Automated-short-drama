# Codex 开发规则

1. 输出精简，只说重点，不做长篇大论。
2. 动手改代码前先读 `docs/` 下的设计文档、实施计划与规则文档。
3. 每次提交只解决一个明确问题，提交前向用户确认；Commit Message 使用中文；推送使用 git bash。
4. 删除任何文件前先备份。
5. 功能实现遵循 TDD：先写失败测试，确认测试失败，再做最小实现，确认测试通过。
6. 文件职责单一，保持模块边界清晰；不创建超大型、高耦合文件。
7. 不实现任务要求之外的业务代码或重构；发现与文档冲突时先向用户确认。
8. 任务完成后运行验证（测试、静态检查、git status），并输出文件变更清单与验证结果。
